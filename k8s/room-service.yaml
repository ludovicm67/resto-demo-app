apiVersion: v1
kind: Service
metadata:
  name: resto-demo-app-room
  namespace: resto-demo-app
spec:
  type: ClusterIP
  selector:
    app: resto-demo-app-room
  ports:
  - port: 80
    targetPort: 80
